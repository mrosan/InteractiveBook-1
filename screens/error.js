import { View, Text } from 'react-native'

function ErrorScreen({ error, stage }) {
	// TODO design and implement
	return <View>
		<Text>An unknown error happened.</Text>
	</View>
}

export default ErrorScreen;
